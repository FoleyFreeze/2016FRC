/*package org.usfirst.frc.team910.robot;

import edu.wpi.first.wpilibj.CANTalon;

public class IntheWay {
	CANTalon shooterArm;
	CANTalon gatherArm;
	if (shooterArm.set(LOAD)){
		gatherArm.pos(LOAD) || gatherArm.pos(STOW);
	}
	if (shooterArm.set(CLOSESHOT)){	
		gatherArm.pos(LOAD);
	}
	if (shooterArm.set(FARSHOT)){
		gatherArm.pos(LOAD) || gatherArm.pos(STOW);
	}
}
}
*/